# Environment variables for PRODUCTION deployment
# This file should be created on the production server as .env.production
# DO NOT commit this file to git with real production values!

# Environment
ENVIRONMENT=production
DJANGO_SETTINGS_MODULE=real_estate_crm.settings_production

# Production Database Configuration - MASTER DATA - READ ONLY DEPLOYMENT!
# This database contains the company's master data and must NEVER be cleared/reset
DB_NAME=django_db_glomart_rs
DB_USER=root
DB_PASSWORD=ZeroCall20!@HH##1655&&
DB_HOST=localhost
DB_PORT=3306

# CRITICAL: Deployment scripts will NEVER modify this database
# Only code, static files, and dependencies will be updated

# Production Django Settings
SECRET_KEY=your-super-secret-production-key-here
DEBUG=False
ALLOWED_HOSTS=sys.glomartrealestates.com,5.180.148.92,localhost

# Production Media and Static Files
MEDIA_ROOT=/var/www/glomart-crm/media
STATIC_ROOT=/var/www/glomart-crm/staticfiles

# Production Email Configuration
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@glomartrealestates.com

# Production Timezone
TIME_ZONE=Africa/Cairo

# SSL/Security Settings (set to True if using HTTPS)
USE_HTTPS=False
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False